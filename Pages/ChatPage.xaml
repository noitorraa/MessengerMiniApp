<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MessengerMiniApp"
             x:Class="MessengerMiniApp.Pages.ChatPage"
             x:Name="chatPage"
             Title="Chat"
             BackgroundColor="{DynamicResource PrimaryColor}">

    

    <Grid RowDefinitions="*, Auto" ColumnDefinitions="*" Padding="10">
        <CollectionView x:Name="MessagesCollectionView" Grid.Row="0" Grid.Column="0" BackgroundColor="DarkSlateGrey" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10" Orientation="Horizontal" VerticalOptions="Center" HorizontalOptions="Start">
                        <StackLayout.Triggers>
                            <DataTrigger TargetType="StackLayout" Binding="{Binding UserID, Converter={StaticResource IsCurrentUserConverter}}" Value="True">
                                <Setter Property="HorizontalOptions" Value="End" />
                            </DataTrigger>
                        </StackLayout.Triggers>

                        <Frame CornerRadius="10" Padding="10" HasShadow="True" BackgroundColor="{Binding UserID, Converter={StaticResource UserIdToColorConverter}}" MaximumWidthRequest="250">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Text="{Binding Content}" TextColor="White" FontSize="16" LineBreakMode="WordWrap"/>

                                <StackLayout Grid.Row="1" Orientation="Horizontal" HorizontalOptions="End" Spacing="5">
                                    <Label Text="{Binding CreatedAt, StringFormat='{0:HH:mm}'}" TextColor="LightGray" FontSize="12"/>

                                    <Label Text="{Binding IsRead, Converter={StaticResource StatusToTextConverter}}"
                                   FontSize="Small"
                                   TextColor="{Binding IsRead, Converter={StaticResource StatusToColorConverter}}"
                                   IsVisible="{Binding UserID, Converter={StaticResource IsOutgoingMessageConverter}}"/>
                                </StackLayout>
                            </Grid>
                        </Frame>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <StackLayout Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Padding="10" BackgroundColor="{DynamicResource SecondaryColor}">
            <Button Text="📎" Clicked="OnAttachClicked" BackgroundColor="Transparent" TextColor="White" FontSize="24" Padding="10"/>
            <Entry x:Name="MessageEntry" Placeholder="Type a message" PlaceholderColor="LightGray" TextColor="White" BackgroundColor="{DynamicResource EntryBackgroundColor}" HorizontalOptions="FillAndExpand" Margin="10,0"/>
            <Button Text="Send" Clicked="OnSendClicked" BackgroundColor="{DynamicResource AccentColor}" TextColor="White" CornerRadius="10" Padding="10"/>
        </StackLayout>
    </Grid>
</ContentPage>